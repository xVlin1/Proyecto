import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ProyectoIonic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
